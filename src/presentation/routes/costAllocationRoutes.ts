import { Router, type RequestHandler } from 'express';
import { CostAllocationController } from '../controllers/CostAllocationController.js';
export function createCostAllocationRoutes(controller: CostAllocationController, requireAuth: RequestHandler): Router { const router = Router(); router.use(requireAuth); router.get('/rules', controller.listRules); router.post('/rules', controller.createRule); router.patch('/rules/:id', controller.updateRule); router.post('/rules/:id/activate', controller.activateRule); router.post('/rules/:id/archive', controller.archiveRule); router.post('/preview', controller.preview); router.get('/summary', controller.summary); router.get('/comparison', controller.comparison); router.get('/unallocated', controller.unallocated); router.get('/resource/:resourceId', controller.resourceSummary); router.get('/export.csv', controller.exportCsv); return router; }

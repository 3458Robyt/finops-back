# Deuda técnica y faltantes — FinOps Inteligente

> Registro autoritativo al 2026-08-12. Cada ítem debe tener estado `ABIERTO`, `BLOQUEADO`,
> `DIFERIDO` o `CERRADO` y evidencia asociada. Los ítems de desarrollo manual no son incidentes.

| ID | Prioridad | Tipo | Estado | Hallazgo / criterio de cierre | Evidencia o siguiente acción |
|---|---|---|---|---|---|
| SEC-001 | Alta | Producción | CERRADO | Verificar técnicamente el enforcement runtime RLS y dejar activación permanente condicionada al despliegue. | Canary 2026-08-03 contra Supabase principal: `finops_runtime`, dos tenants, worker context y cross-tenant cero. Activación permanente/rollback operativo quedan documentados y diferidos hasta tener destino de despliegue. |
| DB-001 | Alta | Supabase | CERRADO | Hardening de funciones y cobertura de índices FK. | 61 migraciones locales; migraciones `202608110010`–`202608110011` y `202608120002`–`202608120005` aplicadas en Supabase; Advisors de seguridad sin lints; 33 índices líderes de FK explícitamente cubiertos más cuatro índices de expiración auth; tablas de asignación, credenciales y helpers FinOps sin ejecución API directa. Los avisos restantes son únicamente índices no usados INFO y se conservan hasta contar con tráfico representativo. |
| ING-001 | Media | Datos | CERRADO | Scheduler no debe encolar conexiones sin validación/capacidades vigentes. | Validación e invalidación implementadas; se conservaron 5 fallos no asociados a prueba. |
| DEP-001 | Media | Dependencias | CERRADO | Reducir carga OCI y eliminar vulnerabilidades de producción. | Módulos OCI específicos 2.138.0; mediana fría ~2,13 s; audit productivo sin vulnerabilidades. |
| AI-001 | Media | Validación de proveedor | CERRADO | Verificar el flujo real de chat y recomendaciones sin persistir datos de prueba. | Canary 2026-08-03 en schema aislado con `gpt-5.4-mini`: chat/recomendaciones en español, auditor, snapshot canónico, rúbrica, trazas, 3 recomendaciones y ahorros no negativos; 54.662 s y 4.093 tokens estimados. |
| AWS-001 | Alta | Validación cloud | BLOQUEADO | Validar STS/EC2/CloudWatch/Cost Explorer/FOCUS con una cuenta y rol AWS reales. | Falta cuenta/rol externo. Las credenciales bootstrap permiten `AssumeRole`; no son credenciales de tenants. |
| OCI-001 | Media | Redundancia cloud | BLOQUEADO | Habilitar canary read-only de OCI Usage API con policy mínima: `Allow group <group_name> to read usage-report in tenancy`. | FOCUS sigue como fuente primaria; falta aplicar policy en IAM OCI. |
| OPS-001 | Media | Operación | DIFERIDO | Workers separables, healthchecks, alertas 24/7 y scheduler productivo. | La separación granular por `APP_PROCESS_ROLE` (`ingestion-worker`, `learning-worker`, `recommendation-analysis-worker`, `savings-reconciliation-worker` y schedulers específicos) está implementada y probada; desarrollo manual, despliegue 24/7 y alertas siguen diferidos hasta definir destino. |
| OPS-002 | Baja | Presupuestos | DIFERIDO | Conectar evaluación periódica de presupuestos a worker/scheduler desplegado. | La evaluación manual funciona durante desarrollo. |
| MSG-001 | Media | Mensajería | ABIERTO | Validar envío real de alertas de presupuesto con proveedores externos. | Código y pruebas unitarias implementados: `OutboundChannelDeliveryService` persiste `SENT`/`SKIPPED`/`FAILED` con errores sanitizados; `OutboundMessageDeliveryProcessor` conserva `PROCESSING`, lease, reintentos y recuperación; SMTP y Telegram aplican `OUTBOUND_PROVIDER_TIMEOUT_MS` (15 s por defecto, 5–60 s en producción); migración `202608110009_outbound_delivery_queue` aplicada y verificada en Supabase. `npm run test:canary:messaging` ya ofrece un canary directo, seguro y omitido por defecto; faltan credenciales/proveedores reales y su ejecución documentada, por lo que el envío externo permanece deshabilitado por defecto. |
| FIN-003 | Media | Analítica FinOps | CERRADO | Generar escenarios de pronóstico comparables sin presentar proyecciones como ahorro realizado. | `CostAnalyticsService.getForecastScenarios`, panel de dashboard y escenarios para base/tendencia/aprobado/ejecutado/verificado; pruebas determinísticas y suite backend vigente. |
| AI-006 | Media | IA/aprendizaje | CERRADO | Permitir revertir una memoria activa sin borrar trazabilidad ni permitir cambios fuera del alcance autorizado. | `PATCH /api/v1/ai/learning/memories/:memoryId/deactivate`; transacción tenant/global autorizada, auditoría `AGENT_MEMORY_DEACTIVATED` y UI de Agente IA. |
| AI-007 | Media | IA/calibración | CERRADO | Medir la calidad observable del ciclo de recomendaciones sin confundir aprobación humana con precisión ML y validar las consultas reales contra PostgreSQL aislado. | `GET /api/v1/agent/quality` calcula por tenant revisión, aprobación/rechazo, abstenciones/evidencia insuficiente, ahorro estimado vs. verificado, desglose por tipo/regla/proveedor y latencia/tokens. Servicio 2/2, autorización 2/2 y `npm run test:integration:agent-quality` 1/1 en schema efímero: migraciones desde cero, dos tenants, separación AWS/OCI y limpieza en `finally`. Ground truth ML y precios LLM siguen explícitamente no inventados. |
| DATA-003 | Media | Datos/IA | CERRADO | Exponer oportunidades de calidad de datos antes de llamar al LLM, sin inventar ahorro ni acciones cloud. | Catálogo `finops-opportunity-rules-v1` en `ResourceLinkageReadinessService`, evidencia de vínculos/frescura/tags y panel en Ingesta; la muestra de recursos se declara explícitamente cuando el endpoint está limitado. |
| OPS-003 | Media | Secretos/observabilidad | DIFERIDO | Secret manager externo, rotación formal, logs/alertas centralizados. | Requerido antes de producción pública. |
| OPS-004 | Media | Observabilidad operativa | CERRADO | Detectar procesos API, worker y scheduler detenidos o atrasados sin inventar un healthcheck HTTP para procesos sin servidor. | `runtime_process_heartbeats` con RLS por `app.worker_id`, estados `RUNNING`/`STOPPED`, intervalo/umbral configurables y parada ordenada; `ProcessHeartbeatService`, runtime de background y migración `202608120005_runtime_process_heartbeats`; integración PostgreSQL aislada validó propietario, aislamiento entre procesos y stale/STOPPED. |
| OPS-005 | Media | Readiness operativo | CERRADO | Readiness debe comprobar BD, rol runtime, migración esperada, capacidad de lease y heartbeat del proceso sin convertir la indisponibilidad opcional de IA en una caída determinística. | `OperationalReadinessService` y `PrismaOperationalReadinessRepository`; `GET /ready` expone checks separados, `DB_EXPECTED_MIGRATION` es obligatorio en producción y la integración aislada verificó migraciones, advisory lease, RLS runtime y heartbeat fresco. |
| OPS-006 | Media | Recuperación | DIFERIDO | Ejecutar un rehearsal formal de backup, restore aislado y medición RTO/RPO en el destino de despliegue autorizado. | `docs/OPERACION_RECUPERACION.md` documenta backup, restore aislado, migraciones, rotación, pérdida de worker, jobs abandonados y rollback RLS; la ejecución productiva requiere proveedor, destino y credenciales operativas reales. |
| FIN-001 | Media | Alcance FinOps | CERRADO | Distribución porcentual auditable, separación por moneda y cierre reproducible sin contabilidad. | Migraciones `202608040001_shared_cost_allocation_closures` a `202608040008_cost_metrics_tenant_period_index`; API/UI DIRECT/SPLIT, Decimal, idempotencia, versiones correctivas, snapshot de líneas, FK tenant-aware, inmutabilidad, grants runtime y preview obligatorio verificados. Chargeback contable sigue fuera de alcance. |
| FIN-002 | Media | Integración FinOps | CERRADO | Consultar presupuesto, impacto y ahorro por destino sin duplicar cálculos ni atribuir evidencia inexistente. | Presupuesto y Value Realization reutilizan cierres cerrados. La atribución exige evidencia exacta de tenant, moneda, recurso canónico, hash de métrica y período; si falta, no se atribuye. Cierres previos a `202608040004` pueden no tener snapshot de líneas. |
| SEC-002 | Media | Escalabilidad | DIFERIDO | Usar un store compartido para rate limiting cuando el backend se despliegue en más de una instancia. | El limitador actual es fijo, seguro y en memoria para desarrollo/instancia única; migrar a Redis o store equivalente antes de escalar horizontalmente. |
| SEC-003 | Media | Toolchain | DIFERIDO | Actualizar vulnerabilidades transitivas del toolchain de desarrollo frontend cuando exista una versión compatible con corrección. | `finops-app` mantiene `npm audit --omit=dev --audit-level=high` en cero; el audit completo reporta 22 vulnerabilidades de dependencias de desarrollo, incluyendo `brace-expansion` y `postcss`, sin fix disponible. No afectan el bundle/runtime de producción. |
| QA-001 | Baja | Entorno | CERRADO | Integración y E2E reproducibles sin Docker local. | Schema Supabase `finops_e2e_goal_20260812` migrado desde cero, integración PostgreSQL 5 archivos/6 pruebas aprobadas y schema eliminado; smoke frontend 1/1. El E2E completo de aplicación requiere levantar API/BD aislados. |
| QA-002 | Media | Validación UI | CERRADO | Smoke autenticado reproducible sin depender de una contraseña real compartida. | Última evidencia: `test:e2e:full` aplicó las migraciones en un schema `finops_e2e_*`, creó credenciales controladas, aprobó 5/5 flujos Playwright y limpió fixtures/schema en `finally`; una nueva ejecución requiere `TEST_DATABASE_URL` de un entorno aislado y no debe usar la BD principal. |
| VAL-001 | Media | Rendimiento | CERRADO | Medir realización de valor con 10.000 recomendaciones y 20.000 mediciones. | Benchmark aislado: resumen 459 ms, página 447 ms, exportación 994 ms, EXPLAIN 131 ms. |
| PERF-001 | Media | Rendimiento | DIFERIDO | Llevar preview y cierre de asignación a los objetivos orientativos de 500 ms y 2 s con 10.000 costos persistidos, cuando exista un entorno representativo de despliegue. | Supabase actual: preview 1.647,36 ms y cierre 6.604,64 ms; 10.000 líneas persistidas sin pérdida ni descuadre. La compuerta revalida además la huella canónica de la fuente. `EXPLAIN (ANALYZE, BUFFERS)` usa `cost_metrics_tenant_period_idx` y tarda 10,35 ms en el plan; insert masivo JSONB y timeout ampliado ya aplicados. Falta ajustar transferencia/infraestructura sin degradar la trazabilidad. |
| PERF-002 | Media | Rendimiento/IA | CERRADO | Aplicar y verificar los índices compuestos que soportan la paginación keyset del reporte de calibración en el entorno remoto. | `npx prisma migrate deploy` aplicó `202608120001_quality_report_keyset_indexes` en Supabase; `npm run db:verify:quality-indexes` encontró ambos índices y `EXPLAIN (COSTS OFF)` confirmó `Index Only Scan Backward` para recomendaciones y trazas con filtros tenant/fecha y límite keyset. |
| LINEAGE-001 | Alta | Datos/IA | CERRADO | Mantener una identidad normalizada y auditable entre inventario, costos, métricas, recomendaciones y corridas de análisis. | Migraciones `202608030001_resource_lineage_normalization`, `202608030002_recommendation_resource_guard`, `202608030003_resource_lineage_readiness_indexes` y `202608030004_analysis_run_canonical_resource`; backfill paginado aplicado; endpoint `/ingestion/resource-linkage`; evidencia y análisis técnico exigen `cloudResourceId` canónico cuando existe ambigüedad. |
| LINEAGE-002 | Alta | Cobertura OCI | CERRADO | Aumentar la cobertura histórica de costos enlazados cuando el inventario OCI no contiene el mismo identificador que FOCUS. | Resource Search, catálogo histórico exacto y backfill idempotente aplicados en Supabase: 11 referencias OCI históricas, 8.173/8.173 costos elegibles enlazados (100 %), 555 identificadores no soportados y 432 costos sin conexión excluidos explícitamente del denominador técnico. Segunda ejecución: cero candidatos y cero actualizaciones; sin fuzzy matching. |
| AUTH-001 | Alta | Sesiones | CERRADO | El logout y la revocación deben invalidar sesiones JWT persistidas sin esperar la expiración natural del token. | Validación de sesión persistida en cada request protegido, logout individual/global, cambio de tenant y revocación de dispositivos; frontend sin localStorage/sessionStorage. Commits `483ff02`, `71381ef`, `5995ef5`. |
| AUTH-002 | Alta | MFA | CERRADO | TOTP privilegiado debe tener recuperación de un solo uso sin almacenar códigos en texto plano ni permitir replay. | `mfa_recovery_codes` conserva solo SHA-256 de códigos aleatorios de 80 bits, batch, uso y revocación; activación/regeneración/consumo son transaccionales, frontend los muestra una sola vez y runtime RLS limita acceso al usuario o desafío. Migraciones `202608110006` a `202608110008`; pruebas 7/7 y canary RLS ampliado aprobado. |
| AUTH-003 | Alta | Autorización | CERRADO | Evitar listas de roles divergentes y probar de forma exhaustiva quién puede ejecutar cada operación sensible. | `AuthorizationPolicy` define siete roles y dieciséis capacidades; rutas y servicios de cloud, ingesta, agente, recomendaciones, ahorro, presupuestos, asignación, valor, mensajería, MFA y administración MSP reutilizan la política. Matriz en `docs/MATRIZ_AUTORIZACION.md`; suite dirigida 45/45 y `test:unit` vigente con 429 pruebas pasadas y 10 omitidas. |
| AUTH-004 | Media | Retención auth | CERRADO | Limpiar sesiones y credenciales efímeras expiradas sin eliminar trazabilidad de negocio ni debilitar replay detection. | `AuthLifecycleCleanupService` y `PrismaAuthLifecycleCleanupRepository` procesan lotes bounded solo por `expiresAt`; scheduler opt-in con `finops-maintenance:auth-lifecycle`; migraciones `202608120002`–`202608120004` restringen RLS a filas expiradas, agregan índices y revocan ejecución a API roles; la selección de sesiones bloquea filas con `FOR UPDATE SKIP LOCKED` y verifica refresh vigentes; integración aislada pasó y limpió en `finally`, conservando refresh tokens futuros incluso con TTL inconsistente. |
| SEC-005 | Alta | RLS autenticación | CERRADO | Las políticas de credenciales no deben generar recursión infinita al ejecutarse como `finops_runtime`. | Helpers SECURITY DEFINER resuelven usuario/sesión pre-auth sin subconsultas RLS cruzadas; canary ampliado cubre sesiones, refresh, reset, MFA, challenges y recovery codes con dos tenants y cero visibilidad cruzada. Migración `202608110008_remove_auth_rls_recursion`. |
| SEC-004 | Alta | Logs | CERRADO | Los errores externos no deben escribir credenciales, tokens, URLs autenticadas, headers de autenticación/cookies ni PEM en logs o campos durables. | `safeErrorMessage` acota y redacta esos materiales en logs, respuestas HTTP, trazas IA, aprendizaje, ingesta, contexto, Telegram e inventario OCI; los controladores críticos comparten `respondWithFinOpsError`. Regresiones de bearer, cookies y persistencia; suite vigente 429 pruebas pasadas y 10 omitidas. |
| SEC-006 | Media | Amenazas | CERRADO | Mantener una matriz STRIDE verificable para las superficies críticas de la beta y distinguir controles implementados de riesgos externos. | `docs/MODELO_AMENAZAS_STRIDE.md` cubre identidad, tenant switch, MSP, cloud, Telegram, SMTP, ingesta, IA, ejecución, asignación, cierres y workers; DAST, rate limit distribuido, secret manager y despliegue público permanecen como riesgos residuales documentados. |
| ING-002 | Media | Inventario OCI | CERRADO | La ingesta OCI debe descubrir compartimentos accesibles recursivamente o declarar la cobertura real, sin presentar inventario parcial como completo. | Identity paginado con cobertura `COMPLETE`/`FALLBACK`/`CONFIGURED_ONLY`/`FAILED`, conteos de compartimentos y recursos en el resultado del job; pruebas del provider 7/7. Commit `5990b87`. |
| ING-003 | Media | Inventario/lineage | CERRADO | El scheduler debe refrescar inventario antes de depender de cruces de costos y métricas, y la calidad de tags debe ser visible. | Job OCI controlado: 1 recurso persistido, 2 llamadas SDK y 3,4 s. Scheduler con validación/capacidades vigentes y cooldown de 24 h; readiness/UI muestran cobertura de `environment`, `owner`, `application` y `cost_center`. Commits `d638269` y `796888c`. |
| AI-002 | Alta | IA/gobernanza | CERRADO | La compuerta determinística debe evitar falsos bloqueos de métricas absolutas y planes/ahorros fuera del recurso o evidencia analizada. | Utilización solo para métricas porcentuales, alcance exacto de cuenta/recurso, reglas versionadas `technical-rules-2026-08-11.v1` con umbrales persistidos en la evidencia, señales auxiliares de red/disco/IOPS y techo de ahorro derivado antes del LLM; golden scenarios 24/24 offline. Commits `bc1072f`, `4c12d5f`, `ca2b634`, `ef73ff8`, `ab53bc6`. |
| AI-004 | Alta | IA/seguridad | CERRADO | El artefacto de plan no debe contener tool calls, SQL, shell, scripts ni payloads ejecutables. | `noExecutablePayload` en la rúbrica determinística, restricción explícita en prompt y regresión golden; `npm run test:ai:offline` 24/24. Los pasos de CLI legibles para ejecución manual siguen permitidos; no existe intérprete de respuestas LLM. |
| AI-005 | Alta | IA/seguridad | CERRADO | El contexto externo no debe cambiar las reglas del agente ni la salida IA debe exponer secretos o material operativo sensible. | Instrucción compartida de contexto no confiable para chat, oportunidades, planes y auditoría; `sensitiveOutputGuard` bloquea PEM, JWT, API keys, URLs autenticadas y asignaciones de secretos antes de trazar/persistir. Golden scenarios y pruebas dirigidas aprobados; IA offline 24/24. |
| AI-003 | Media | IA/aprendizaje | CERRADO | El feedback debe ser observable y no confundirse con la aprobación del auditor; una memoria solo se incorpora después de auditoría válida. | `/api/v1/ai/learning/summary` agrega decisiones humanas, estados `PENDING/APPROVED/REJECTED/SKIPPED/ERROR` y memorias activas por tenant en PostgreSQL; UI de Agente IA muestra el resumen. `AgentLearningService` queda coordinador, el procesador auditable separado y el cierre aprobado es atómico. Commits `347d175`, `4e74693`, `4b5bab1`, `5b59b5a`, `bd1bd5f`. |
| MOD-001 | Media | Mantenibilidad | CERRADO | Dividir los hotspots críticos por responsabilidad sin introducir duplicación, ciclos ni cambios de contrato; no imponer troceo mecánico a módulos cohesivos. | La composición, ingesta, métricas, IA, repositorios de valor, mensajería y frontend fueron separados por responsabilidad. `OutboundMessageService` quedó en 268 líneas al extraer `OutboundChannelDeliveryService` (114 líneas), la configuración de fuentes cloud en `CloudConnectionSourceConfiguration` (120 líneas), la facturación OCI en `OciBillingCollector` (183 líneas), el backfill técnico en `CloudIngestionBackfillService` (106 líneas) y la generación de recomendaciones en `FinOpsAiRecommendationRunner` (153 líneas), sin cambiar contratos HTTP ni estados. Los contratos `IRecommendationRepository` e `ICloudConnectionRepository` componen capacidades cohesivas; `npm run check:architecture` pasa con 356 archivos de producción y una única excepción documentada para `goldenScenarios.ts` (fixture declarativo IA). Los módulos restantes de 200–400 líneas son responsabilidades cohesivas y su extracción queda como mantenimiento oportunista, no como bloqueo de la beta. |
| REL-001 | Baja | Release | CERRADO | La higiene de archivos sensibles debe ser una compuerta reproducible y no depender solo de una revisión manual de `.gitignore`. | `npm run check:release-hygiene` inspecciona las rutas rastreadas y bloquea secretos, certificados, bases locales, logs y artefactos E2E; backend validó 598 archivos y frontend 134. `.env.example` es la única excepción permitida. |

## Componentes permanentes del roadmap

1. Gobernanza de releases y configuración.
2. Higiene de datos y jobs operativos.
3. Mantenimiento periódico de Supabase y Advisors.
4. Rendimiento de dependencias y arranque.
5. Calificación periódica del proveedor IA.
6. Operación productiva activable cuando exista destino de despliegue.

## Decisiones de alcance

- FOCUS continúa como fuente operativa primaria; CPU, memoria, red y disco provienen de Monitoring/CloudWatch.
- AWS real y OCI Usage API requieren permisos/cuentas externas; no se simulan para cerrar deuda.
- No se implementa remediación automática cloud.
- Los workers se ejecutan manualmente durante desarrollo.
- El grafo visual fue retirado por baja utilidad y latencia.
- Los documentos históricos no son fuentes de estado; consultar `docs/ESTADO_ACTUAL_FINOPS.md`,
  `docs/ROADMAP_PRODUCTO.md` y `PROGRESO_ROADMAP_FINOPS.md`.

## Historial de cierre

- 2026-08-03: canaries runtime RLS, IA real y OCI read-only cerrados técnicamente; integración de trazabilidad 5/5 con mediana de readiness 186,46 ms; activación productiva permanente diferida por falta de despliegue.
- 2026-07-31: hardening Supabase, índices FK, limpieza controlada, scheduler validado y reducción de OCI.
- 2026-07-28: beta integrada con contexto tenant, RLS runtime y workers seguros.
- 2026-07-26: Centro de Realización de Valor y benchmark.
- 2026-07-11: evidencia técnica canónica, auditor IA y aprendizaje por recurso.
